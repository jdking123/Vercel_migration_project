# Custom Component Registration

## Purpose
Allow teams to create custom components without breaking the system.

## Requirements
- Must use core tokens
- Must use CVA for variants
- Must follow naming conventions
- Must include accessibility notes
- Must include documentation
- Must pass extension validation

## Registration
- Custom components must be registered in the extension registry
- Must include version number
- Must include owner information
